import metTasks from '../backend/addtask'

describe('Testing addTasks functions', () => {

  test('adds one element to the DOM', () => {
    expect(functs.stringLength('Wharever')).toBe(8);
  });  
})