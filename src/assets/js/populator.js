const propPopulator = {
  container: document.getElementsByTagName('ul'),
  descr: localStorage.getItem('descr'),
  status: localStorage.getItem('status'),
  index: localStorage.getItem('index'),
}

export const metPopulator = {
  fill(obj) {
    
  }
}